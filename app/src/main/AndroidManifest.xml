<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ict.choose">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Choose">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MemberShipActivity" android:label="회원가입"/>
        <activity android:name=".MainPageActivity" android:label="카테고리"/>
        <activity android:name=".WriteActivity" android:label="글 쓰기"/>
        <activity android:name=".LoveActivity" android:label="연애"/>
        <activity android:name=".HelthActivity" android:label="운동"/>
        <activity android:name=".TripActivity" android:label="여행"/>
        <activity android:name=".ShoppingActivity" android:label="쇼핑"/>
        <activity android:name=".FashionActivity" android:label="패션"/>
        <activity android:name=".DreamActivity" android:label="진로"/>
        <activity android:name=".MyWriteActivity" android:label="내가 쓴 글"/>
        <activity android:name=".ChangePwdActivity" android:label="비밀번호 재설정"/>
    </application>

</manifest>